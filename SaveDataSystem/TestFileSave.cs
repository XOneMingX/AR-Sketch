using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestFileSave
{
    //[SerializeField]
    ////public List<Line> lines;
    //public List<Generates> generate;
    //public List<ListLayer> listLayer;
    //public List<DrawLayer> drawLayer;

    ////SaveFile class Initializer which takes in a LineRenderer List and stores it as a Line class list.
    //public TestFileSave(List<GameObject> listLayers, List<GameObject> drawLayers)
    //{
    //    //lines = new List<Line>();
    //    //generate = new List<Generates>();
    //    listLayer = new List<ListLayer>();
    //    drawLayer = new List<DrawLayer>();

    //    //foreach (LineRenderer lineRenderer in lineRenderers)
    //    //{
    //    //    lines.Add(new Line(lineRenderer));
    //    //}
    //    //foreach(GameObject objects in generates)
    //    //{
    //    //    generate.Add(new Generates(objects));
    //    //}
    //    foreach(GameObject getListLayer in listLayers)
    //    {
    //        listLayer.Add(new ListLayer(getListLayer));
    //    }
    //    foreach(GameObject getDrawLayer in drawLayers)
    //    {
    //        drawLayer.Add(new DrawLayer(getDrawLayer));
    //    }

    //}

    ////public void Draw()
    ////{
    ////    foreach (Line line in lines)
    ////    {
    ////        line.Drawing();
    ////    }
    ////}

    ////public void ReGenerate()
    ////{
    ////    foreach (Generates objects in generate)
    ////    {
    ////        objects.Generating();
    ////    }
    ////}

    //public void buildListLayer()
    //{
    //    foreach (ListLayer layers in listLayer)
    //    {
    //        layers.layerBuild();
    //    }
    //}

    //public void buildDrawLayer()
    //{
    //    foreach (DrawLayer layers in drawLayer)
    //    {
    //        layers.layerBuild();
    //    }
    //}
}
